import styles from "@/app/styles/modules/components/footer.module.scss";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <div className={styles.footer__container}>Developed with Love ❤️</div>
    </footer>
  );
};

export default Footer;
